/**
 * Twitter/X card. Identical to the Open Graph card — the two differ only in
 * which meta tag points at them, so re-exporting keeps one drawing rather than
 * letting a second copy drift.
 */
export { alt, size, contentType, default } from './opengraph-image';
